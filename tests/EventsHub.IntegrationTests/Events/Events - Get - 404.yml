info:
  name: Events - Get - 404
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/events/:eventid"
  params:
    - name: eventid
      value: non-existing-id
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
